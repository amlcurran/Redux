<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="match_parent"
    android:layout_width="match_parent">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <FrameLayout
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:id="@+id/image_action_host">

            <view
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                class="com.espiandev.redux.view.FixedImageView"
                android:id="@+id/asset_image_view"
                android:scaleType="centerCrop" />
        </FrameLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:divider="@drawable/divider_vertical_bright"
            android:dividerPadding="12dp"
            android:showDividers="middle">

            <Button
                android:drawableTop="@drawable/ic_button_play"
                android:text="@string/play"
                android:id="@+id/button_play"
                style="@style/Button.Action" />

            <Button
                android:drawableTop="@drawable/ic_button_download"
                android:text="@string/download"
                android:id="@+id/button_download"
                style="@style/Button.Action" />

            <Button
                android:drawableTop="@drawable/ic_button_cast"
                android:text="@string/cast"
                android:id="@+id/button_cast"
                style="@style/Button.Action" />

            <!--<Button-->
                <!--android:drawableTop="@drawable/ic_button_share_default"-->
                <!--android:text="@string/share"-->
                <!--android:id="@+id/button_share"-->
                <!--style="@style/Button.Action" />-->

        </LinearLayout>

        <LinearLayout
            android:orientation="vertical"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:padding="@dimen/default_padding">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/asset_description"
                style="@style/Banner.Low" />

        </LinearLayout>

    </LinearLayout>
</ScrollView>